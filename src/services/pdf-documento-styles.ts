/** CSS exclusivo dos PDFs — injetado no iframe de captura, sem Tailwind/mobile. */
import pdfDocumentoCss from '@/components/os/os-documento.css?raw'

export const PDF_DOCUMENTO_CSS = pdfDocumentoCss
